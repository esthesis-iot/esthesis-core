export interface DashboardItemDatetimeConfigurationDto {
  date: boolean;
  time: boolean;
  local: boolean;
  server: boolean;
  formatDate: string;
  formatTime: string;
  formatDateTime: string;
}
