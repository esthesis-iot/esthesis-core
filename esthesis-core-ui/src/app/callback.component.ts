import {Component} from "@angular/core";

/**
 * An empty component that is used to handle the callback from the OpenID Connect provider.
 */
@Component({
  selector: "app-callback",
  template: ""
})
export class CallbackComponent {

}
