package esthesis.platform.common.config;

/**
 * The unit in which a device information is expressed. Note: Never remove or rearrange the order of the enum's elements
 * as to save bandwidth during transmission the ordinal value is used.
 */
public enum DeviceInfoUnit {
  STRING,
  PERCENTAGE,
  INTEGER,
  DECIMAL
}
