Thank you for installing esthesis IoT platform.

{{ if .Values.global.devEnv.enabled }}

*************************
* DEV ENVIRONMENT SETUP *
*************************
Update your hosts file with the result of the following command:

echo $(kubectl get services camunda-optimize --output jsonpath='{.status.loadBalancer.ingress[0].ip}') esthesis-dev-camunda-optimize
echo $(kubectl get services camunda-identity --output jsonpath='{.status.loadBalancer.ingress[0].ip}') esthesis-dev-camunda-identity
echo $(kubectl get services camunda-operate --output jsonpath='{.status.loadBalancer.ingress[0].ip}') esthesis-dev-camunda-operate

{{ end }}
