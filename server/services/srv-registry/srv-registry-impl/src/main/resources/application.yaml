quarkus:
  application:
    name: esthesis-srv-registry
  container-image:
    group: esthesisiot
    name: esthesis-srv-registry
  mongodb:
    database: esthesis
    credentials:
      username: esthesis
      password: esthesis
  liquibase-mongodb:
    migrate-at-start: true
  devservices:
    enabled: false
  log:
    level: warn
    category:
      esthesis:
        level: INFO
    console:
      format: "%d{HH:mm:ss} %-5p traceId=%X{traceId}, parentId=%X{parentId}, spanId=%X{spanId}, sampled=%X{sampled} [%c{2.}] (%t) %s%e%n"
  opentelemetry:
    enabled: false
